# Agent Notes

Use `CLAUDE.md` as the main project memory for this repository.

Before changing the dashboard, run or review `npm test` so you know the contract. After changing `index.html`, `data.json`, the deploy workflow, or any dashboard-critical copy, run `npm test` again.

Keep edits focused on making the static research dashboard clearer, more polished, and harder to misread. Do not soften the research-only and failed-validation disclosures.
